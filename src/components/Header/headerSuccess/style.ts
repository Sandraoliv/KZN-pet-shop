import styled from "styled-components";

export const SccHeader = styled.header`
  display: flex;
  align-items: center;
  justify-content: center;

  height: 60px;

  background-color: var(--color-tertiary-transparent);

  box-shadow: 2px 2px 2px 1px rgba(0, 0, 0, 0.1);
`;
